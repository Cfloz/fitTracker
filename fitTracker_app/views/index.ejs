<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>
    <nav>
      < a href="/workouts/new">Create a new Workout</a>
    </nav>
    <h1>FitTracker App</h1>
    <form action="/user/logout?_method=DELETE" method="POST">
      <input type="submit" value="Logout" />
    </form>
    <h2>Welcome, <%=user.username%>!</h2>
    <h3>Workouts</h3>
    <% for(let i = 0; i < workouts.length; i++){ %>
      <p><%=workouts[i].workout%></p>
      <p><%=workouts[i].reps%></p>
      <p><%=workouts[i].weight%></p>
      <p><%=workouts[i].date%></p>
      <p><%=workouts[i].notes%></p>
      <form action="/workouts/<%=workouts[i].id%>?_method=DELETE" method="POST">
        <input type="submit" value="Delete Workout" />
      </form>
      <form action="/workouts/<%=workouts[i].id%>/edit" method="GET">
        <input type="submit" value="Edit Workout" />
      </form>